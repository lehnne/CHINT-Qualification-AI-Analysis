# 任职资格 AI 分析 · 当前项目进度

> 本文档记录项目当前状态、设计决策、踩过的坑和个人习惯偏好，便于后续继续推进。

---

## 一、项目背景

**项目名称**：任职资格 AI 分析系统

**平台**：飞书 Aily（支持 Agent + 知识库 + 工作流 + MCP）

**目标**：对任职资格平台中每个人的关键成果和项目经历进行 AI 分析，生成：
1. **个人能力标签** — 按人员 + 岗位序列 + 原职级分类
2. **金标签** — 按岗位序列 + 职级 + 维度，标识区分通过/未通过的关键能力
3. **劣势诊断与精准提问** — 针对个人相对同岗位同职级人员的短板生成面试问题

**项目目的**：准备面试 AI 应用开发产品经理岗位，此项目作为面试作品展示专业能力。

**GitHub 仓库**：https://github.com/lehnne/CHINT-Qualification-AI-Analysis

---

## 二、术语约定

| 术语 | 说明 |
|------|------|
| **岗位序列** | 即之前说的"职系"。**往后统一叫"岗位序列"**，如技术研发类、产品类、设计类等 |
| **原职级** | 员工当前职级，如 P5、P6 |
| **申报职级** | 员工本次评审申报的目标职级 |
| **金标签** | 同岗位序列 + 同职级中，能区分通过/未通过的关键能力标签 |
| **D 系数** | 区分度系数，D = (通过组标签均分 - 未通过组标签均分) / 未通过组标签均分 |

---

## 三、个人习惯与偏好

> 以下是我在这项目中形成的习惯和偏好，后续工作请遵循。

### 3.1 命名习惯

- **岗位序列**：统一叫"岗位序列"，不叫"职系"
- **SQL 变量**：分页偏移量用 `@start`，不用 `@Offset`（避免与 SQL 关键字冲突）
- **JSON 字段**：英文 key + 中文 value，如 `position_family_name = "技术研发类"`
- **接口风格**：能合并就合并，倾向单一合并接口而非多个小接口

### 3.2 文档习惯

- SQL 查询语句单独整理一个 md 文件，不混在设计文档里
- Agent Prompt 单独提取为 md 文件，方便复制到飞书 Aily
- 设计文档用 HTML 格式（可视化效果好，适合面试展示）
- 文档中用表格对比，少用长段落

### 3.3 技术偏好

- 数据库是 **SQL Server**，所有查询用 T-SQL 语法
- 分页用 `OFFSET @start ROWS FETCH NEXT @PageSize ROWS ONLY`（SQL Server 2012+）
- 可选筛选条件用 `AND (@Param IS NULL OR Column = @Param)` 模式
- 评审活动 ID（`rn_code`）为**可选参数**，不填则查全部
- 写入接口用 **PUT**（幂等设计，标签数据可重复生成）
- 维度配置作为**可配置元数据**存在数据库中，不写死在 Prompt 里

### 3.4 沟通习惯

- 沟通语言为**中文**
- 喜欢先确认需求再动手，不喜欢直接改
- 修改要保持多个文件的一致性（HTML 设计文档、SQL 手册、Agent Prompt）

---

## 四、踩过的坑

### 4.1 PostgreSQL → SQL Server 全部重写

**问题**：最初所有 SQL 都按 PostgreSQL 语法写的（UPSERT、LIMIT/OFFSET、pgvector 等），后来才知道任职资格平台用的是 SQL Server。

**教训**：开工前先确认数据库类型。现在 SQL 手册和设计文档中的源数据库查询已全部改为 T-SQL，但 AI 分析系统自有数据库（PostgreSQL）部分保留 PG 语法。

**影响范围**：SQL 手册（3 个查询全部重写）、设计文档 10.8 节 Part A/B/C。

### 4.2 SearchReplace 大块替换失败

**问题**：尝试用 SearchReplace 替换设计文档中约 190 行的 SQL 块时，报 `deserialize params error: missing field`。

**解决**：改用子智能体（Task subagent）来执行大块编辑，或者拆成多个小块分别替换。

**教训**：SearchReplace 的 old_str 不宜过长，尽量控制在 50 行以内。

### 4.3 章节编号漂移

**问题**：在设计文档中插入新的第二章"业务逻辑澄清"后，后续所有章节编号都需要 +1，包括 h2 标题、目录链接、子章节编号。

**教训**：插入新章节时，要同步更新所有引用该章节编号的地方（目录、交叉引用、文案描述）。

### 4.4 GitHub Fine-grained PAT 权限不足

**问题**：首次推送时 403 权限被拒，fine-grained PAT 只有 `metadata=read` 权限，缺少 Contents 写入权限。

**解决**：在 GitHub Settings → Personal Access Tokens 中，将 Repository permissions 的 **Contents** 设为 **Read and Write**。

**教训**：创建 fine-grained PAT 时，除了选对仓库，还要逐一检查每个 permission 的读写级别。

### 4.5 Git Push 被拒（远程已有内容）

**问题**：GitHub 创建仓库时自动初始化了 README，导致首次 push 被 reject（`fetch first`）。

**解决**：`git push --force` 强制推送覆盖远程内容。

### 4.6 职系 → 岗位序列 术语替换

**问题**：用户中途要求将"职系"统一改为"岗位序列"，但设计文档 HTML 中有大量"职系"字样。

**当前状态**：SQL 手册和 Agent Prompt 已全部更新。**设计文档 HTML 中仍有部分"职系"未替换**，后续修改时需注意。

---

## 五、当前成果

### 5.1 已完成的文件

| 文件 | 说明 | 状态 |
|------|------|------|
| `qualification-ai-analysis.html` | 完整 11 章设计文档（架构设计、Prompt 设计、MCP 工具、数据库、接口、面试策略） | ✅ 完成 |
| `任职资格平台查询SQL手册.md` | 3 个 SQL Server 查询接口完整 SQL + 字段映射 | ✅ 完成 |
| `智能体Prompt-标签生成Agent.md` | 能力标签生成 Agent 的完整提示词 | ✅ 完成 |
| `当前项目进度.md` | 本文件 | ✅ 完成 |
| `assets/charts.js` | ECharts 配置（桑基图、雷达图、柱状图） | ✅ 完成 |
| `_shared/` | 字体文件 + echarts.min.js | ✅ 完成 |
| GitHub 仓库 | https://github.com/lehnne/CHINT-Qualification-AI-Analysis | ✅ 已推送 |

### 5.2 设计文档章节结构（11 章）

| 章节 | 标题 | 内容 |
|------|------|------|
| 01 | 项目概览 | 核心设计原则 |
| 02 | 业务逻辑澄清 | 维度体系、标签提取逻辑、金标签逻辑、数据流全景 |
| 03 | 系统架构设计 | 双轨架构、数据流 |
| 04 | MCP 工具链详细设计 | 9 个 MCP 工具定义、JSON Schema |
| 05 | HR 分析 Agent System Prompt | 系统提示词设计 |
| 06 | 核心分析 Prompt 模板 | 维度分类、标签生成、金标签识别、劣势诊断 4 个模板 |
| 07 | 预生成 Pipeline 设计 | 飞书 Aily 工作流设计 |
| 08 | 金标签识别机制详解 | D 系数数学定义、识别流程 |
| 09 | 劣势诊断与精准提问 | 雷达图示例、诊断输出示例 |
| 10 | 数据存储与接口规范 | 数据库设计、写入/查询接口、SQL 手册 |
| 11 | 面试展示策略 | 话术、技术选型问答、简历建议 |

### 5.3 已完成的 MCP 工具

**用户已开发完成**：
- `query_assessment_materials` — 合并查询接口，一次性拉取评审活动下所有申报人员 + 关键成果 + 项目经历

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `rn_code` | string | 否 | 评审活动ID，不填则查所有评审活动 |
| `post_code` | string | 否 | 岗位序列代码 |
| `lev_code` | string | 否 | 职级代码 |
| `page_size` | int | 否 | 每页行数，默认 100 |
| `start` | int | 否 | 偏移量，第一页为 0 |

**写入 MCP 工具（已设计完成）**：
- `write_employee_tags` — 将Agent生成的能力标签写入数据库，调用方式：POST

**设计中尚未实现**（共 8 个）：

预生成 Pipeline MCP（2 个）：
1. `fetch_latest_assessments` — 获取每人最新评审记录
2. `get_dimension_schema` — 获取岗位序列的维度定义

HR 分析 Agent MCP（6 个）：
1. `get_capability_tags` — 检索某员工能力标签
2. `get_gold_tags` — 获取金标签
3. `get_position_benchmark` — 获取岗位基准画像
4. `compare_with_peers` — 对标分析
5. `get_weakness_questions` — 生成短板提问
6. `get_employee_assessment_history` — 历史评审轨迹

### 5.4 已完成的 Agent Prompt

文件：`智能体Prompt-标签生成Agent.md`

**核心设计**：
- 5 步工作流：拉取数据 → 按 (employee_id, position_family_code, original_grade) 分组 → 维度分类 → 每个维度生成 1-3 个标签 → 输出结构化 JSON
- 标签含：tag_name, score(0-100), confidence(0-1), evidence, source_materials
- 评分标准：90-100 卓越 / 80-89 优秀 / 70-79 良好 / 60-69 达标 / <60 待提升
- 5 条约束：只处理已完结评审、不保留历史版本、证据可追溯、维度映射唯一、批量输出

---

## 六、关键设计决策

| 决策 | 选择 | 理由 |
|------|------|------|
| 标签生成时机 | 预生成 + 定时更新 | 成本控制（批量复用上下文）、体验优化（秒级响应）、一致性保障 |
| 标签分组键 | 人员 + 岗位序列 + 原职级 | 同一人可能评审不同岗位序列；原职级代表能力基线 |
| 金标签分组键 | 岗位序列 + 职级 | 按岗位分组样本太小，按岗位序列+职级保证统计显著性 |
| 维度管理 | 可配置元数据（数据库存储） | 新增岗位序列只需更新配置，不改代码 |
| 接口合并 | 单一合并接口（UNION ALL） | Pipeline 不用循环调用，一次拉完 |
| 写入接口用 PUT | 幂等设计 | 标签数据可重复生成，PUT 天然幂等 |
| 字段命名 | 英文 key + 中文 value | 英文 key 便于程序处理，中文 value 便于 AI 理解 |
| 分页变量名 | `start`（非 `offset`） | 避免与 SQL 关键字冲突 |
| 评审活动筛选 | 可选（非必填） | 支持全量查询和按活动查询两种模式 |
| SQL 方言 | SQL Server T-SQL | 任职资格平台使用 SQL Server |

---

## 七、源数据库结构（SQL Server）

### 源数据库表（任职资格平台，只读）

| 表名 | 说明 | 核心字段 |
|------|------|---------|
| `TB_ZC_ReviewApply` | 职称申报申请表头 | RACode(申报编号), RNCode(评审活动ID), EmplySFCode(工号), PostCode(岗位序列代码), LevCode(申报职级), ReviewLevel(审批结果), Status(状态: 4=通过/5=不通过), OldPostName(当前岗位), OldPostLev(原职级), PostName(岗位序列名称) |
| `TB_ZC_EmplyOtherInfo` | 人员其他信息表 | EmplySFCode(工号), EmplyName(姓名), PostLev(职级) |
| `TB_ZC_WorkProduct` | 关键成果表 | EmplySFCode, KeyEvents(关键事件), EventDes(事件描述), WorkAchievements(工作成果), Skill(专业能力), TypeRole(所扮角色) |
| `TB_ZC_ProjectInfo` | 主导或参与项目情况表 | EmplySFCode, ProjectName(项目名称), ProjectDesc(项目介绍), ProjectRole(项目角色), ProjectResult(项目贡献度描述) |

**关键字段值**：
- `Status`：4 = 通过，5 = 不通过
- `ReviewLevel`：审核通过 / 审核不通过
- 所有表都有 `Deleted` 字段（软删除标记）

### AI 分析系统数据库（自有，PostgreSQL）

4 张表，字段描述如下：

#### 1. `employee_capability_tags`（个人能力标签表）

存储每个人在每次评审中各维度下的能力标签。每行 = 一个人在某次评审中某个维度下的一个标签。

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGSERIAL | 自增主键，无业务含义 |
| `employee_id` | VARCHAR(50) | 员工工号，来自任职资格平台 |
| `employee_name` | VARCHAR(50) | 员工姓名（冗余字段，方便查询和展示，避免联表） |
| `position_family_code` | VARCHAR(20) | 岗位序列代码（如：DYJS15），对应任职资格平台码表 PostCode |
| `position_family_name` | VARCHAR(50) | 岗位序列名称（如：技术研发类），对应任职资格平台码表 PostName |
| `original_position` | VARCHAR(100) | 原岗位名称（如：后端开发工程师） |
| `original_grade` | VARCHAR(20) | 原职级（如：P5），对应 TB_ZC_ReviewApply.OldPostLev |
| `target_grade` | VARCHAR(20) | 申报目标职级（如：P6） |
| `assessment_cycle` | VARCHAR(20) | 评审周期标识（如：2025-H1），用于区分不同周期的评审 |
| `dimension` | VARCHAR(50) | 能力维度名称（如：技术专长、技术创新） |
| `tag_name` | VARCHAR(100) | 能力标签名称（如：复杂架构设计、高并发优化） |
| `score` | SMALLINT | 标签得分 0-100，基于材料中的量化成果和角色重要性综合评定 |
| `confidence` | DECIMAL(3,2) | 置信度 0.00-1.00，描述越具体、量化越清楚则置信度越高 |
| `evidence` | TEXT | 生成该标签的依据摘要（来自具体材料中的哪段描述） |
| `source_materials` | JSONB | 支撑该标签的材料ID列表，格式：["MAT_001","MAT_002"] |
| `batch_id` | VARCHAR(50) | 写入批次ID，用于追溯本次 Pipeline 的运行批次 |
| `created_at` | TIMESTAMPTZ | 创建时间，自动生成 |
| `updated_at` | TIMESTAMPTZ | 更新时间，自动生成 |

#### 2. `gold_tags`（金标签表）

存储按岗位序列 + 职级 + 维度聚合计算出的金标签。Pipeline 从 `employee_capability_tags` 中聚合计算，按"通过组"和"未通过组"分别统计每个标签的平均分，算出 D 系数。

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGSERIAL | 自增主键，无业务含义 |
| `position_family_code` | VARCHAR(20) | 岗位序列代码（如：DYJS15），对应任职资格平台码表 PostCode |
| `position_family_name` | VARCHAR(50) | 岗位序列名称（如：技术研发类），冗余存储方便展示 |
| `grade` | VARCHAR(20) | 职级（如：P6），金标签按岗位序列 + 职级分组计算 |
| `dimension` | VARCHAR(50) | 能力维度，金标签在每个维度下独立计算 |
| `tag_name` | VARCHAR(100) | 金标签名称，与 employee_capability_tags 中的 tag_name 对应 |
| `distinction_coef` | DECIMAL(5,3) | D 系数区分度 = (通过组均分 - 未通过组均分) / 未通过组均分，D>0.25 有区分意义 |
| `penetration_rate` | DECIMAL(4,3) | P 系数穿透率 = 通过组持有该标签人数 / 通过组总人数，P>0.6 表示该标签在通过组中普遍存在 |
| `avg_score_passed` | DECIMAL(5,2) | 通过组在该标签上的平均得分 |
| `avg_score_failed` | DECIMAL(5,2) | 未通过组在该标签上的平均得分 |
| `passed_count` | INT | 通过组中有该标签的人数 |
| `failed_count` | INT | 未通过组中有该标签的人数 |
| `is_core` | BOOLEAN | 是否核心金标签，D >= 0.30 且 P >= 0.50 标记为核心金标签 |
| `is_verified` | BOOLEAN | HRBP 是否已人工确认此金标签生效 |
| `batch_id` | VARCHAR(50) | 写入批次ID，用于追溯 Pipeline 运行记录 |
| `updated_at` | TIMESTAMPTZ | 更新时间，每次 Pipeline 全量覆盖时更新 |

#### 3. `position_benchmarks`（岗位基准画像表）

存储每个岗位序列 + 职级下各维度标签的统计分布，用于个人能力对标分析。

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGSERIAL | 自增主键，无业务含义 |
| `position_family_code` | VARCHAR(20) | 岗位序列代码（如：DYJS15），对应任职资格平台码表 PostCode |
| `position_family_name` | VARCHAR(50) | 岗位序列名称（如：技术研发类），冗余存储方便展示 |
| `grade` | VARCHAR(20) | 职级（如：P6），基准画像按岗位序列 + 职级分组统计 |
| `dimension` | VARCHAR(50) | 能力维度，基准画像在每个维度下独立统计 |
| `tag_name` | VARCHAR(100) | 标签名称，与 employee_capability_tags 中的 tag_name 对应 |
| `mean_score` | DECIMAL(5,2) | 平均得分，反映该标签在同岗位序列同职级中的平均水平 |
| `median_score` | DECIMAL(5,2) | 中位数 P50，反映典型水平，比均值更抗极端值影响 |
| `std_dev` | DECIMAL(5,2) | 标准差，反映得分离散程度，标准差大说明该标签水平差异大 |
| `percentile_25` | DECIMAL(5,2) | 25 分位数 P25，反映下四分位水平 |
| `percentile_75` | DECIMAL(5,2) | 75 分位数 P75，反映上四分位水平 |
| `sample_count` | INT | 样本量，参与统计的人数，样本量太小（如 < 10）则基准参考价值有限 |
| `batch_id` | VARCHAR(50) | 写入批次ID，用于追溯 Pipeline 运行记录 |

#### 4. `dimension_schema`（维度定义配置表）

配置表，定义每个岗位序列有哪些能力维度。HR 在后台配置，代码读取后动态注入 Prompt。这是系统的元数据层，新增岗位序列只需加一行配置，不需要改代码。

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGSERIAL | 自增主键，无业务含义 |
| `position_family_code` | VARCHAR(20) | 岗位序列代码（如：DYJS15），关联任职资格平台码表 |
| `position_family_name` | VARCHAR(50) | 岗位序列名称（如：技术研发类），冗余存储方便展示 |
| `dimension_name` | VARCHAR(50) | 维度名称（如：技术专长、技术创新） |
| `description` | TEXT | 维度描述，说明该维度考察什么能力，用于 AI 理解维度含义 |
| `sort_order` | SMALLINT | 展示排序 1-5，控制维度在页面和 Prompt 中的展示顺序，越小越靠前 |
| `typical_evidence` | TEXT | 该维度下典型的能力证据示例，用于 Prompt 的 Few-shot 示例 |
| `is_active` | BOOLEAN | 是否启用，可临时禁用某个维度而不删除数据 |

> 完整 DDL 及 COMMENT 见 `pg数据库建表脚本.sql`。

---

## 八、业务逻辑要点

### 评审周期
- 每两年一次，员工自主申报目标岗位序列 + 职级
- 勾选项目经历和关键成果作为申报材料
- 评审委员会评审 → 通过 / 不通过

### 不同岗位序列的能力维度

| 岗位序列 | 能力维度 |
|---------|---------|
| 技术研发类 | 技术专长、技术创新、客户导向、知识技能 |
| 产品类 | 产品规划、用户洞察、数据分析、商业思维、项目管理 |
| 设计类 | 设计方法论、用户研究、技术实现、审美判断、行业视野 |
| 市场类 | 市场洞察、策略制定、创意能力、渠道管理、效果评估 |
| 管理类 | 团队管理、战略规划、资源协调、绩效驱动、人才培养 |

### 个人能力标签提取逻辑

```
分组键 = (employee_id, position_family_code, original_grade)
```
- 同一个人、同一个岗位序列、同一个原职级的材料合并为一组
- 只取最新一次评审记录，不跨周期合并
- 每条材料归入一个能力维度 → 每个维度下生成 1-3 个标签

### 金标签生成逻辑

```
分组键 = (position_family_code, grade, dimension)
```
- 按岗位序列 + 职级分组（如"技术研发-P6"）
- 组内分为通过组(A)和未通过组(B)
- 在每个维度下逐标签计算区分度 D
- 判定条件：D > 0.25 且 通过组该标签均分 > 65 → 金标签

---

## 九、待完成工作

| 序号 | 任务 | 优先级 | 说明 |
|------|------|--------|------|
| 1 | 金标签识别 Agent Prompt | 高 | 单独提取为 md 文件 |
| 2 | 劣势诊断 Agent Prompt | 高 | 单独提取为 md 文件 |
| 3 | HR 分析 Agent System Prompt | 中 | 设计文档中有设计，未单独提取为 md |
| 4 | HTML 设计文档"职系"→"岗位序列"全局替换 | 中 | SQL 手册和 Agent Prompt 已完成 |
| 5 | 飞书 Aily 平台实际部署与调试 | 中 | 需要平台环境 |
| 6 | 维度配置表 `dimension_schema` 初始数据填充 | 低 | 需要确认各岗位序列的完整维度列表 |
| 7 | Pipeline 工作流在飞书 Aily 中搭建 | 低 | 需要平台环境 |

---

## 十、用户背景

- 3 年 AI 应用开发经验，曾从 0 到 1 带领 35,000 人的 HR Agent
- 正在准备 AI 应用开发产品经理岗位面试
- 此项目是面试作品，需要体现：Prompt 工程能力、MCP 设计能力、业务理解力、产品架构思维
- 技术平台：飞书 Aily（支持 Agent + 知识库 + 工作流 + MCP）
- 任职资格平台数据库：SQL Server
- AI 分析系统数据库：PostgreSQL（设计阶段，尚未搭建）
